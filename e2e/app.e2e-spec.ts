import { UsersCloudPage } from './app.po';

describe('users-cloud App', () => {
  let page: UsersCloudPage;

  beforeEach(() => {
    page = new UsersCloudPage();
  });
  //
  // it('should display message saying app works', () => {
  //   page.navigateTo();
  //   expect(page.getParagraphText()).toEqual('app works!');
  // });
});
